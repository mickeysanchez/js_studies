// Write your own myBind method. Add it to Function.prototype. This is trivial to do if you use Function#apply.
// Assume the method you're binding doesn't take any arguments; we'll see tomorrow how to use the special arguments to fix this.

Function.prototype.myBind = {
  
}